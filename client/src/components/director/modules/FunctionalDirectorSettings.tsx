// 📱 MOBILE-OPTIMIZED Director Settings Management
import React from 'react';
import UnifiedSchoolSettings from './UnifiedSchoolSettings';

// Alias component for dashboard integration
const FunctionalDirectorSettings: React.FC = () => {
  return <UnifiedSchoolSettings />;
};

export default FunctionalDirectorSettings;